all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    webservers:
      hosts:
        managed_node:
          ansible_connection: ssh
          ansible_user: kali
          ansible_host: 10.0.2.15
